
  # GradPath Build

  This is a code bundle for GradPath Build. The original project is available at https://www.figma.com/design/U9KT4vA66KBqcHTqWgjv3h/GradPath-Build.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  